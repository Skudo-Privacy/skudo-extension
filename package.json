{
  "name": "skudo-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Skudo browser extension.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "format": "prettier --write \"src/**/*.{js,css,vue}\" \"test/**/*.js\""
  },
  "devDependencies": {
    "happy-dom": "^20.0.0",
    "prettier": "^3.5.3",
    "vitest": "^4.0.0"
  }
}
